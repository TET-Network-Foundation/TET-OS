# 2026-05-30 (土) Sprint 4 Day 4 — Block_id schema V2 hard fork

## Summary

深夜 00:00-00:45 の 45 分間で、Phase 0 ship blocker の chain consensus core bug を発見・診断・修正・deploy・verify した。block_id schema が height + tx_hashes だけで決まる設計欠陥 (空 coinbase block で fork hidden) を、parent_block_id + state_root + producer_id を含む V2 schema にアップグレード。hard fork で chain reset、両 node が height 111 で完全 state_root 一致を継続。Phase 1 mainnet (2026-09-15) 後では実施不可能な fix を、Phase 0 testnet 中に間に合わせた。

## Key Achievements

### 1. Chain state divergence の root cause 確定

3 回の Cursor forensic 調査の末:

Root cause: block_id = SHA256(height || tx_hashes) (consensus.rs:370-376)
- parent_block_id, state_root, producer_id を含まない
- 空 coinbase block では block_id が height だけの関数
- 同 height で異なる state を持つ chain が IDENTICAL block_id を生成
- parent linkage check 通る
- divergence は tip の state_root check で初めて発覚
- catch-up tip path に reorg recovery なし → 永久 reject loop

前 2 回の Cursor 仮説 (JSON 再注入、dev_faucet) は両方とも fact ベースで否定。Cursor 自身が retract。

### 2. Block_id V2 schema 実装 (chain consensus hard fork)

新 schema:
- TET_BLOCK_ID_V2| prefix (V1 非衝突保証)
- height (LE8)
- parent_block_id
- state_root
- tx_hashes
- producer_id

Genesis: parent = GENESIS_ZERO_PARENT_BLOCK_ID (32 zero bytes hex)
Mining 順序入替: state preview (non-destructive) → state_root → block_id → undo → apply
compute_state_root_after_remote_block で circular dependency 解消

実装変更:
- consensus.rs: signature 変更 + 4 production callsite
- tests.rs: 10 test callsite (auto-recalculate)
- cargo build --release: PASS
- cargo test: 101/101 PASS

### 3. Hard fork deploy + 2-node sync 完全成功

- VPS DB reset (rm -rf /opt/tet-core-data)
- VPS rebuild (1m02s incremental) + restart
- 新 PeerId: 12D3KooWAEwhEw449dtWaACn238vykWVC8GqRRkKGDovKVnP5JAn
- Mac DB reset + tet_ledger.json 削除
- Mac rebuild + restart

Verification (00:42 CEST):
- VPS height 66 / Mac height 66 / state_root 完全一致
- catch-up applied: 41 blocks
- REMOTE BLOCK APPLIED: 8 blocks (リアルタイム gossip)
- state_root mismatch: 0
- apply rejected: 0

Verification (00:45 CEST, 3 分後):
- VPS height 111 / Mac height 111 / state_root 完全一致
- 連続 sync 維持
- 0 errors

### 4. その他副次 fix の動作再確認

朝の multi-swarm port fix と夜の chain_hello + blacklist TTL も新 chain で動作確認:
- 3 swarm port 分離 (4001/4003/4005)
- 周期 chain_hello resend 動作
- block-plane 経由の sync 確実

## Lessons (教訓)

- Block identity に state を含めない設計は危険。Bitcoin/Ethereum が parent + state を block hash に含めるのは、divergence の即座検出 + chain choice の根拠。TET の旧 schema は空ブロックで height だけになるエッジケースを見逃していた。
- Cursor を間違いから学ばせる prompt design が重要。3 回目の調査で前 2 回の誤りを明示し、fact ベースを強制した結果、Cursor 自身が前仮説を retract できた。AI 調査の信頼性は prompt 設計次第。
- Phase 0 testnet は chain core 変更のための window。Phase 1 mainnet 後では block_id schema 変更は実行不可能 (full chain reset 必要)。Phase 0 中の hard fork は cost ゼロ、benefit 巨大。
- 深夜の judgment muscle: 朝の Steve なら "止める" を選んだかもしれないが、今夜は Cursor の forensic 3 回で root cause が見える状態だった。深夜でも fact が並んでいれば判断できる。

## Commits (this session)

- 767fee9 fix: periodic chain_hello + blacklist TTL for catch-up resilience
- e7c77c4 docs: daily log for 2026-05-28 + 2026-05-29
- 283430f docs: chain state divergence bug investigation log
- c6e63a1 fix: block_id schema V2 - chain consensus hard fork (CORE)
- b59595b docs: clean up literal text in BUG investigation log
- a7e7bce docs: daily log 2026-05-30 (placeholder, content added later)

## Next Steps

1. "synced":false の minor display bug 調査 (機能的影響なし)
2. /tmp/tet_ledger.json を TET_DB_DIR 配下に移す (Option A、Cursor 推奨済み)
3. Faucet を新 chain で再 enable + production test
4. Sovereign OS UI 着手
5. 長期 idle test (10 時間 sync 維持確認)
