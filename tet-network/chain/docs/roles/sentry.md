# Role: sentry

## 目的

バリデータやアーカイブの「直接露出」を避けるため、外部の P2P 接続を受ける境界ノードとして動作させる。

## 必要ポート（例）

- P2P: `30333`
- RPC: （原則不要、あるいは最小限）
- Prometheus: node 設定に準ずる

## 必須フラグ（最小）

- `--chain tet_testnet`
- バリデータ/アーカイブの鍵や RPC を直接公開しない

## 永続データ/バックアップ

- できるだけスナップショット復元で復旧できる前提に寄せる

