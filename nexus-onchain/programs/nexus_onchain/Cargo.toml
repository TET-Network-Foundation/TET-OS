[package]
name = "nexus_onchain"
version = "0.1.0"
description = "Nexus on-chain staking (dev SOL stake)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "nexus_onchain"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"

