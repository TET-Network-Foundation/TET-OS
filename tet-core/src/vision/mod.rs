//! Phase 0–2 whitepaper alignment: **scaffolding only** (types, traits, REST probes).
//! Ledger settlement paths remain unchanged until migration flags are enabled.

pub mod caac;
pub mod fluid_net;
pub mod pqc_bridge;
pub mod thermo_genesis;
pub mod zk_court;
